apply plugin: "war"

bootWar {
    mainClassName = "de.uni_stuttgart.it_rex.quiz.QuizServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
